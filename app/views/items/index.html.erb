
<div class="p-10 max-w-screen-sm">
  <%= turbo_stream_from "items"%>
  <h1 class="mb-4 text-4xl text-center text-green-900 font-sans tracking-widest">
    TODO
  </h1>
  <%= render "items/form", item: @item %>
  <%= turbo_frame_tag "items" do %>
      <%= render @items %>
  <% end %>
  <div class="text-gray-500">
    <%= @completed_count %> of  
    <%= turbo_stream_from "total_count" %>
    <%= turbo_frame_tag "total_count" do %>
      <%= render 'total_count' %>
    <% end %> 
    items
  </div>
</div>